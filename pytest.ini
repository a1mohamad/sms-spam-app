[pytest]
testpaths = tests
addopts = -v --basetemp=.pytest_tmp
markers =
    integration: tests that require real artifacts/models
    database: tests that require a migrated PostgreSQL database
    smoke: process-level checks that verify the application can start and respond
    deployment: tests that require a running containerized deployment
